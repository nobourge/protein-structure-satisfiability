from folder import *

# generate all sequences of 0 to 30 characters
# and compute their score max
# and store each sequence in the list containig the sequences with
# the same matrix size



# sequences with a score maximized in a matrix of size sqrt(len(seq))
sqrt = []
# sequences with a score maximized in a matrix of size sqrt(len(seq))
sqrt_1 = []
# sequences with a score maximized in a matrix of size sqrt(len(seq))
sqrt_2 = []


for i in range(0, 31):
